export enum GameMode {hamster
	"daily",
	"hourly",
	"infinite",
	// "https://mikhad.github.io/wordle/#hamster/1",
};

export const enum ms {
	SECOND = 1000,
	MINUTE = 1000 * 60,
	HOUR = 1000 * 60 * 60,
	DAY = 1000 * 60 * 60 * 24,
};
